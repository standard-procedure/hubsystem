name: "hubsystem"

services:
  server:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
    - ../server:/workspaces/hubsystem/server:cached
    command: sleep infinity
    environment:
      DB_HOST: postgres
    depends_on:
      postgres:
        condition: service_healthy

  world:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
    - ../world:/workspaces/hubsystem/world:cached
    env_file: devcontainer.env
    command: sleep infinity
    environment:
      DB_HOST: postgres
      HUBSYSTEM_HOST: http://server:3000
      OLLAMA_HOST: http://ollama:11434
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: pg_isready -U postgres
      interval: 10s
      timeout: 5s
      retries: 5
  
  sandbox:
    image: ubuntu:24.04
    restart: unless-stopped
    volumes:
      - sandbox-workspaces:/workspaces/sandbox
    command: sleep infinity

  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    volumes:
      - ollama-data:/root/.ollama
  
volumes:
  postgres-data:
  sandbox-workspaces:
  ollama-data:
